apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-pipeline-config
data:
  config.yaml: |
    # Default config settings
    cdf:
      authentication:
        apiKey:
        apiKeyGcp: ""

    source:
      rawDb: "sap-extract"
      table: "notification"